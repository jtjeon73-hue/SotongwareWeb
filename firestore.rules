rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
  // 클라이언트 직접 write 금지 — Functions Admin SDK만 접근
    match /contactInquiries/{id} {
      allow read, write: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
